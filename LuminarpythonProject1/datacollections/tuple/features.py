# tuple can keeps the element in order
# tuple has heterogenous character
# tuple support duplication
# immutable (updation does't possible)
# indexing is possible in tuple
# tuple lenght is calculated using len(l) function
# nesting is possible (support another list in same list)